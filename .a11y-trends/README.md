# Accessibility Trend Data

This directory stores audit trend data for tracking accessibility improvements over time.

- `history.json` — Audit history (auto-generated, git-ignored)
- CI artifacts upload this data for cross-run persistence

To view trends: `npm run trends`
To generate an HTML report: `npm run trends:report`
